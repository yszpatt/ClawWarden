# 更新日志 (CHANGELOG)

本文档记录了 ClawWarden 项目的主要里程碑和深度技术更新内容。

---

## [2026-02-15] - 交互逻辑增强与视觉风格重塑

### 交互流转优化
- **审核控制与状态回转**:
    - 为处于 `pending` (待审核) 状态的任务引入了 **Approve (通过)** 和 **Reject (拒绝)** 双重操作。
    - 拒绝操作现可将任务状态安全重置回 `idle`，允许开发者在不移动泳道的情况下进行调整。
    - 针对 `failed` (执行失败) 任务新增了 **Retry (重试)** 按钮，实现一键状态重流转。
- **操作面板布局重构**: 引入了响应式的按钮网格布局 (Grid System)，解决了多操作按钮并存时的遮挡和错位问题，界面组织更具条理。

### 🛠️ MCP 工具与流程集成
- **原生任务创建工具**:
    - 引入了 `create_task` MCP 工具，允许 Claude Agent 在执行过程中自主创建子任务或后续任务到任意指定泳道。
    - 在 MCP 工具流中集成了自动化 **Git 工作树管理**；由 MCP 创建的新任务会立即获得独立的开发环境。
    - 增强了任务元数据，支持回溯关联，明确任务是由哪个 Agent 会话通过 MCP 创建的。

### 🎨 视觉系统进化 (The New Zinc Theme)
- **专业级深色模式**: 将 UI 色调从早期的发光效果全面切换为更冷峻、更具工业感的 **Zinc (锌色系 900/950)**。这种扁平化的配色方案大幅降低了视觉疲劳，提升了专业度。
- **全局背景网格 (Global Grid)**: 移除了单一背景，引入了基于 CSS 路径的轻量化网格覆盖层。这种设计在项目选择器和主工作区营造了“设计画布”般的科技感。
- **卡片化详情布局**: 为任务详情面板引入了 `.detail-card` 容器标准，将不同的信息单元（总结、方案、测试）进行视觉上的物理区隔，显著增强了复杂信息的易读性。

### 🔄 实时同步机制与系统健壮性
- **事件驱动的 UI 刷新**:
    - 将更新触发逻辑下沉到最底层的 `JSONStore`。任何数据修改（创建任务、变更状态、重新排序）现在都会立即触发全项目广播。
    - 移除了对 OS 级别文件变化的强依赖，实现了看板数据的秒级无感刷新。
- **鲁棒的 WebSocket 管理**:
    - **持久化项目订阅**: 增强了 `ConnectionManager` 以记录项目订阅状态。
    - **自动重连恢复**: 实现了断线重连后的订阅自动核验，确保在开发模式热更新或网络波动后，实时更新能力依然可用。
- **错误处理与稳定性**:
    - 统一了后端的广播逻辑，消除了冗余消息。
    - 修复了任务详情面板中与消息输入组件相关的关键编译错误。

---

## [2026-02-13] - Markdown 体验深度优化与稳定性增强

### 🎨 Markdown 渲染优化
- **视觉美化**: 全面提升了任务详情页中 Markdown 元素的视觉表现，包括更清晰的标题层级、优化的代码块样式、精美的表格布局以及更合理的列表间距。
- **样式隔离**: 引入了独立的 `MarkdownRenderer.css` 样式模块，实现了 Markdown 渲染逻辑与样式定义的解耦，增强了可维护性。

### 🐞 Bug 修复与稳定性
- **停止按钮可靠性**: 修复了页面刷新后“停止执行”按钮可能失效的问题，确保在任何状态下都能可靠控制任务。
- **状态同步优化**: 优化了任务停止或完成后的 UI 状态流转逻辑，确保按钮状态和进度指示器能够实时、准确地反映后端状态。

---

## [2026-02-12] - 自动化流程闭环与总结系统强化

### 🚀 自动化流程流转
- **全泳道自动流转能力**: 修复了之前流转逻辑仅支持计划泳道的局限性。现在，「开发」和「测试」等非计划泳道在配置了 `onCompleteLane` 后，也能在自动化执行完成后自动跳转至目标泳道。
- **任务状态一致性**: 确保任务在完成自动移动、文件保存等后置动作后，状态能可靠地从 `running` 回归为 `idle`，消除了界面状态卡死的问题。

### 📊 结构化总结系统增强 (Structured Summaries)
- **计划方案自动重建 (Fallback Mode)**: 针对 Agent SDK 有时仅返回结构化数据而忽略 Markdown 文本的问题，引入了“重建模式”。系统会自动从 JSON 中的总结、技术方案和组件设计字段拼装生成 Markdown 文档，确保「计划方案」永远有内容。
- **总结看板 UI 进化**: 
    - **多维度展示**: 总结视图现在能够同时呈现 `summary` 概述和详细详情，信息密度更高。
    - **开发详情补完**: 在开发总结中增加了对「新增测试」、「破坏性变更」和「后续步骤」的显式渲染。
    - **测试报告增强**: 增加了代码覆盖率（行、函数、分支）的专项展示。
- **实时反馈**: 在对话列表中增加了「已生成自动总结」的系统级提示，并实现了总结 Tab 的精准实时刷新。

### 🛠️ 系统健壮性优化
- **并发写入保护**: 在 `execution.ts` 中引入了“即时读取-修改-回写”机制，有效解决了在任务快速流转过程中，多个异步保存操作可能导致的 `tasks.json` 数据覆盖或丢失风险。
- **对话面板重置**: 修复了切换任务卡片时，旧任务的计划内容或总结数据残留在新任务面板中的 UI Bug。

### 🤖 AI 辅助合并与环境隔离强化
- **AI 代理合并协议 (AI-Assisted Merge)**: 重构了「待合并」泳道的执行逻辑。现在由 Claude Agent 担任发布工程师角色，负责验证变更、执行 Git 合并、甚至在发生冲突时尝试自主修复，替代了之前的纯脚本合并模式。
- **环境隔离自动化**: 
    - **隔离增强**: 现在在创建 Worktree 时，系统会自动在工作树根目录写入 `.gitignore` 以屏蔽 `.claude/` 目录，彻底解决了拷贝进去的配置文件被误识别为 Git 变更的问题。
    - **自动回收**: 在 Agent 完成合并任务后，可以根据提示词指令自动删除临时工作树和任务分支，实现了环境的生命周期自动化管理。

### 🐞 Bug 修复与稳定性增强
- **总结生成闭环**: 修复了「开发」和「测试」泳道不生成摘要的问题。现已引入文本兜底（Fallback）机制，即使模型未返回结构化 JSON，也能根据执行过程文本自动生成并持久化总结，并解决了「计划」泳道摘要重复生成的问题。
- **任务停止(Stop)可靠性**:
    - **崩溃修复**: 修复了点击“停止”按钮可能导致的后台 null 指针崩溃。
    - **状态同步**: 解决了任务停止后数据库状态未更新、UI 刷新后按钮状态错误的 Bug。
    - **会话持久化**: 现在即使刷新页面，也能通过 WebSocket 重新关联并控制（停止）正在运行的任务。
- **数据一致性修复**:
    - **防止任务消失**: 修复了停止操作误清空任务 `laneId` 导致卡片在看板隐藏的问题。
    - **状态覆盖保护**: 解决了对话执行结束瞬间，`awaiting-review` 状态可能被旧有的 `idle` 逻辑错误覆盖的竞争风险。

### 🎨 配置驱动的交互一致性 (Configuration-Driven UI)
- **动态图标映射 (Dynamic Icon Mapping)**: 前端按钮图标不再硬编码。现在 UI 会解析 `laneconfig.json` 中的 `buttonIcon` 字段，支持从 `palette`, `package`, `code`, `shield-check`, `git-merge`, `play` 等图标库中动态加载。
- **UI 逻辑去重**: 移除了前端代码中针对特定泳道的硬编码按钮逻辑，所有交互动作现在完全由项目级的 `laneconfig.json` 驱动，实现了 UI 与逻辑的高度解耦。

---

## [2026-02-10] - 泳道体验优化与工具权限调整

### 🔧 工具权限调整
- **全泳道工具权限开放**: 移除了计划泳道的只读限制，现在所有泳道都开放完整的工具权限（`Bash`, `Read`, `Edit`, `Glob`, `Grep`, `Find`, `Write`），用户可以在任何阶段使用完整的 Claude Code 能力。

### 🐛 Bug 修复

#### 任务状态同步问题
- **跨泳道状态重置**: 修复了切换任务卡片时按钮状态不正确的问题。现在使用 `task.status` 作为唯一数据源，确保按钮状态始终与实际任务状态同步。
- **对话内容刷新**: 修复了从其他泳道切换到计划泳道时对话内容没有刷新的问题。`useConversation` hook 现在会在 `taskId` 变化时立即清空旧消息。
- **运行时按钮禁用**: 当任务正在运行时，编辑和删除按钮现在会被正确禁用，防止误操作。

#### 泳道任务创建问题
- **创建位置修正**: 修复了无论从哪个泳道创建任务都会出现在计划泳道的问题。现在任务会创建在点击的泳道中。

### 📐 设计文档
- **泳道配置化重构方案**: 新增 `docs/plans/2026-02-10-lane-configuration-refactoring.md`，分析了当前泳道实现的差异，提出了配置驱动的统一重构方案。

---

## [2026-02-04] - 核心交互模型重构与 UI/UX 飞跃

今天我们对项目的核心交互模型进行了革命性的重构，从传统的“原始终端输出”模式全面转向“结构化摘要与对话”模式。

### ✨ 核心功能重构 (Core Refactoring)

#### 1. 结构化增量摘要系统 (Incremental Structured Summaries)
- **技术实现**: 废弃了单次覆盖的摘要写入方式，重构了 `json-store.ts` 中的读写逻辑。现在支持在本地 `.clawwarden` 目录下为每个任务维护一个摘要序列，通过追加方式实现进度的历史溯源。
- **前端展示**: 在 `StructuredOutput.tsx` 中引入了层次化的折叠式（Accordion）视图。
    - **自动索引**: 摘要按时间戳和阶段自动分类。
    - **支持 Markdown**: 集成了 `react-markdown` 和 `remark-gfm`，使得任务产出（如计划方案、测试报告）能以专业、美观的文档格式呈现。
    - **交互优化**: 默认展开最新进度，旧进度可手动折叠，大幅降低了用户获取关键信息的视觉负担。

#### 2. “对话即控制”模型 (Conversational Interaction)
- **模型转变**: 弱化甚至移除了独立的命令行输入，将所有交互统一在 `ConversationPanel.tsx`。
- **流式响应**: 深度集成 Claude Agent SDK 的 `query` 和 `startTaskExecution` 接口，支持毫秒级的消息流式推送，提供如同原生 ChatGPT 的流畅体验。
- **状态感知**: 对话列表能够感知任务状态（正在执行、已停止、已完成），并在界面上通过不同的状态指示灯实时反馈。

### 💄 视觉与交互体验升级 (UI/UX Hyper-focus)

#### 1. 全局设计语言进化
- **风格定义**: 确立了“高科技、专业、冷色调”的视觉基调。
- **细节增强**: 
    - 引入了深度的玻璃拟态（Glassmorphism）效果，利用 `backdrop-filter` 实现了极具层次感的半透明面板。
    - 优化了深色主题的对比度，确保在长时间使用下依然保持良好的可读性。
    - 实现了更为灵动的过渡动画，包括任务卡片的拖拽重排和详情面板的滑动切入。

#### 2. 泳道同步逻辑 (Lane Synchronization)
- **语义化映射**: 通过映射 `Lane` 配置，实现了每个阶段的图标、颜色和按钮的一致性。
    - **计划 (Plan)**: 调色板紫色，使用 `Palette` 图标，操作按钮为“生成计划方案”。
    - **开发 (Develop)**: 调色板蓝靛色，使用 `Code2` 图标，操作按钮为“进入自动化开发”。
    - **测试 (Test)**: 调色板青色，使用 `ShieldCheck` 图标，操作按钮为“开始自动化测试”。
    - **合并 (Merge)**: 调色板灰色，使用 `GitMerge` 图标，操作按钮为“合并到主分支”。

### 🔄 术语统一与流程体验进化 (Terminology & Workflow Evolution)
- **从“设计”到“计划” (Design to Plan)**: 全面将“设计”泳道更名为“计划”泳道。这不仅是 UI 文案的更替，也包括了后端路由、WebSocket 消息协议、文件存储目录（`.clawwarden/plans`）以及数据迁移逻辑的同步更新。
- **协同交互增强**:
    - **可中止的计划生成**: 现在支持在生成计划方案过程中随时点击“停止生成”，实现了与开发泳道一致的交互控制。
    - **工具调用流式可见**: 计划生成过程中，Agent 的工具调用（如 Read, Glob, Grep）现在会以实时流的形式反馈在界面上，让用户清晰感知方案设计的全过程。
    - **预设 Prompt 优化**: 更新了开发和测试泳道的默认提示词，使其更加简洁高效。

### 🗑️ 系统瘦身 (Performance Optimization)

- **移除 Xterm 依赖**: 考虑到开发者并不总是需要查看原始的调试日志，我们移除了庞大的 `xterm` 库及相关的 `node-pty` 后端组件。
- **内存优化**: `ConnectionManager.ts` 不再在内存中缓存海量的原始终端字符流，转而仅处理高价值的消息和状态包，显著降低了前端在处理长时任务时的内存占用。

---

## [2026-02-03] - 灵活性与项目管理增强

### 📂 项目管理
- **动态导入路径**: 实现了 `FolderPicker` 组件，支持用户从本地文件系统任意位置导入现有项目，而不局限于固定目录。
- **项目持久化**: 重构了 `global.json` 的存储结构，支持对项目列表进行实时编辑和远程删除同步。

---

## [2026-02-02] - 品牌升级与架构奠基

### 🦀 品牌重塑
- **ClawWarden**: 项目正式更名，"Claw" 代表 Claude Code，“Warden” 代表监控者/守卫，寓意为智能化的开发助手提供可视化的管控面板。

### 🧱 架构设计
- **Worktree 隔离机制**: 确立了每个任务独立 Git Worktree 的运行模式，解决了 Claude Code 在并发执行任务时可能产生的文件锁和环境污染问题。
- **Monorepo 结构**: 完成了 `web`、`agent`、`shared` 三个包的工程化搭建，实现了类型安全的全栈共享。

---

## [2026-01-30 至 2026-02-01] - 从 0 到 1 的突破

- **WebSocket 通信层**: 搭建了全双工连接，实现了 Agent 状态和消息的实时分发。
- **基础看板**: 实现了基于 `@dnd-kit` 的看板拖拽交互。
- **SDK 对接**: 完成了与 Anthropic Claude Agent SDK 的初步握手和会话保持。
